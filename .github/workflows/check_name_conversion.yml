name: BranchCheck

on:
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    if: startsWith(github.head_ref, 'task/')
    runs-on: ubuntu-latest  
    steps:
      - run: echo "Validate..."
